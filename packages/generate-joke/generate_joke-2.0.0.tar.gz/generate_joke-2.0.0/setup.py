from setuptools import setup

setup(
    name='generate_joke',
    version='2.0.0',
    author='Gautam Sagar Mallela',
    author_email='gautamsagar.mallela@gmail.com',
    description='This Module is used to fetch a random joke from the server',
    packages=[],
    install_requires=[
        # List any dependencies your package requires
        "requests"
    ],
)
