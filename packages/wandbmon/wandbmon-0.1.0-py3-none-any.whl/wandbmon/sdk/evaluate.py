def evaluate(df: "pandas.DataFrame", config: dict) -> dict:
    """Evaluate a dataframe of metrics against a config.

    Args:
        df (pandas.DataFrame): A dataframe of metrics.
        config (dict): The config to evaluate against.

    Returns:
        dict: A dictionary of evaluation results.
    """
    pass