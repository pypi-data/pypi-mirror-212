from setuptools import setup, find_packages

setup(
    name='object-serialization',
    version='0.1.1',
    description='Python serialization package for python objects',
    packages=find_packages(),
    install_requires=[],
    py_modules=['MySerializer'],
)
