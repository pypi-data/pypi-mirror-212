[tool.poetry]
name = "cli-package"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{include = "cli_package"}]

[tool.poetry.dependencies]
python = "3.7.6"

[tool.poetry.scripts]
cli-package = "cli_package.main:main_cli"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
