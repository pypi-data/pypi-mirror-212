[project]
name = "openkeys"
version = "0.0.2"
description = ""
authors = [
    { name = "Yevgnen Koh", email = "wherejoystarts@gmail.com" },
]
dependencies = [
    "requests>=2.31.0",
]
requires-python = ">=3.8"
readme = "README.md"

[project.license]
text = "MIT"

[build-system]
requires = [
    "pdm-backend",
]
build-backend = "pdm.backend"

[tool]
