[tool.poetry]
name = "enumhandler"
version = "0.2.0"
description = """\
A class that allows the definition of handlers for an enum class, along with\
definition-time type and exhaustivity checks.\
"""
authors = ["asthasr <466533+asthasr@users.noreply.github.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = ">=3.8"


[tool.poetry.group.dev.dependencies]
pyright = "^1.1.312"
black = "^23.3.0"
pytest = "^7.3.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
