from setuptools import setup, find_packages
setup(
    name='graphce',
    version='0.0.1',
    description='creation of database by entering type(IP or PHONE or EMAIL) and location',
    author='authsafe',
    packages=find_packages()
)