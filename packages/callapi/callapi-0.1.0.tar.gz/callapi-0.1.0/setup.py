from setuptools import setup

setup(
    name='callapi',
    version='0.1.0',
    description='A module for making API calls',
    packages=['callapi'],
    install_requires=[
        'requests',
    ],
)
