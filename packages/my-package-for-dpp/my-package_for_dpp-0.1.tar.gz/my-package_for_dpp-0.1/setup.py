from setuptools import setup, find_packages


setup(
    name="my-package_for_dpp",
    version="0.1",
    packages=find_packages(),
    description="A sample Python package",
    install_requires=[],
)
